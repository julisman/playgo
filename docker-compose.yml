version: '2'
services:
  apigateway:
    container_name: apigateway
    image: golang
    volumes:
      - ./:/go/src/github.com/user/api-gateway/
      - ./go/src:/go/src
    working_dir: /go/src/github.com/user/api-gateway/
    ports:
      - 9090:9090
    tty: true